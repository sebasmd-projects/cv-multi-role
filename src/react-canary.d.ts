/// <reference types="react/canary" />

// `<ViewTransition>` viaja en el canal canary de React, que es el que usa el
// App Router (Next empaqueta ese React él mismo). Los tipos publicados lo
// dejan aparte, así que hay que pedirlos a mano.
